{
  "name": "apiproxy",
  "version": "1.0.0",
  "description": "An API Proxy to allow all my APIs to work within my network",
  "main": "index.js",
  "scripts": {
    "build": "npm build",
    "clean": "rm -rf node_modules",
    "reinstall": "npm run clean && npm install",
    "rebuild": "npm run clean && npm install && npm run build",
    "start": "node index",
    "dev": "nodemon Server/index",
    "debug": "nodemon --inspect index.js"
  },
  "author": "Onik Noor",
  "license": "ISC",
  "devDependencies": {
    "nodemon": "^2.0.6"
  },
  "dependencies": {
    "dotenv": "^8.2.0",
    "express": "^4.17.1",
    "express-rate-limit": "^5.2.3",
    "express-slow-down": "^1.4.0",
    "http-proxy": "^1.18.1",
    "mysql2": "^2.2.5",
    "serve-favicon": "^2.5.0"
  }
}
