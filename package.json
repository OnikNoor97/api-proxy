{
    "name": "api-proxy",
    "version": "1.0.0",
    "private": true,
    "scripts": {
        "dev": "concurrently --kill-others \"cd ./Client && npm run dev\" \"cd ./Server && npm run dev\""
    },
    "devDependencies": {
        "concurrently": "^7.0.0"
    }
}
